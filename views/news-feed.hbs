<div class="container">
    {{#each data}}
        <div class="card">
            <img src="{{this.img}}" alt="">
            <h1>{{this.title}}</h1>
            <p>{{this.bio}}</p>
        </div>
    {{/each}}
</div>